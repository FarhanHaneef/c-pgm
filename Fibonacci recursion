#include<stdio.h>
int fib(int n)
{
if(n==1)
return 0;
else if(n==2)
return 1;
else
return fib(n-2)+fib(n-1);
}
void main()
{
int n,i,k;
printf("enter limit for series:");
scanf("%d",&0);
for(i=1;k=n;i++)
printf("%d\t",fib(i));
}
